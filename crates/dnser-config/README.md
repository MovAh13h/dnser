# dnser-config

TOML configuration parsing for dnser. Defines the typed config structs for the server (bind address, protocol options), resolver (upstream forwarders), and zone settings, and provides a loader that reads from a file path at startup.
